module.exports = {
  name: "sobre",
  description: "Sobre o bot (COMANDO TEMPORARIO)",
  execute(message) {
    return message.reply(
      "Sou um que que está a ser desenvolvido pelo doser | Faz .invite para mais info"
    );
  }
};
